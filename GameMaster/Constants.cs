using System;

namespace GameMaster
{
    public static class Constants
    {
        public static readonly TimeSpan GameFullCheckInterval = TimeSpan.FromSeconds(1);
        public static readonly TimeSpan GameFullCheckStartDelay = TimeSpan.FromSeconds(2);
    }
}